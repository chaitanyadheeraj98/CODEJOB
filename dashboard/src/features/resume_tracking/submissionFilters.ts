import type { FilterFieldConfig, FilterValues, SortOption } from '../../components/FilterSortBar'
export const submissionFilterFields:FilterFieldConfig[]=[{key:'q',label:'Search',type:'text'},{key:'company',label:'Company',type:'text'},{key:'recruiter',label:'Recruiter',type:'text'},{key:'opportunity_domain',label:'Domain',type:'text'},{key:'implementation_partner',label:'Implementation Partner',type:'text'},{key:'end_client',label:'End Client',type:'text'},{key:'has_premium_contact',label:'In Premium Numbers',type:'boolean'},{key:'tracked',label:'Tracked (has opportunity)',type:'boolean'}]
export const submissionSortOptions:SortOption[]=[{value:'newest',label:'Newest first'},{value:'oldest',label:'Oldest first'},{value:'next_action',label:'Next action due'}]
export const submissionDefaultFilterValues:FilterValues={q:'',company:'',recruiter:'',opportunity_domain:'',implementation_partner:'',end_client:'',has_premium_contact:null,tracked:null}
export function submissionFiltersToParams(v:FilterValues){const p:Record<string,string>={};for(const key of ['q','company','recruiter','opportunity_domain','implementation_partner','end_client']){const value=v[key] as string;if(value?.trim())p[key]=value.trim()}if(v.has_premium_contact!=null)p.has_premium_contact=String(v.has_premium_contact);if(v.tracked!=null)p.tracked=String(v.tracked);return p}
