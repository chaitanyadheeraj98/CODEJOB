# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment files
.env*

# pytest caches
.pytest_cache/
.testmondata*

# end_client remediation audit trail. The applied run and PROVENANCE.md are
# committed - they are the record of rows destroyed in production. Dry runs are
# reproducible by re-running the script, so they are not.
var/end_client_remediation/*-dryrun.json
